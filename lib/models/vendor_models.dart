// ตัวอย่างโมเดลสำหรับข้อมูลร้านค้า
class store {
  final String vendorId;
  final String shopName;
  final bool isOpen;

  store({
    required this.vendorId,
    required this.shopName,
    required this.isOpen,
  });
}
